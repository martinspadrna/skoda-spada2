# RaK v.1.5 (954) – Dashboard: vycentrování řádku kdo chybí

- Horní směnový panel měl centrovaný hlavní text, ale subřádek s absencemi mohl zůstat opticky mimo střed.
- Přidána finální CSS vrstva pro `.dashboardHeroLine3`, `.dashboardHeroLine3Pill`, `.dashboardHeroLine3Main` a `.dashboardHeroLine3Sub`.
- Přepsané jsou i starší `transform` posuny, aby centrování drželo napříč displeji.
