# RaK v.1.5 (916) – Datum i čas v Piškvorky Top score

Tabulka „Kdo porazil AI“ nově zobrazuje sloupec `Datum a čas`.

Uložení výsledku do `gomoku_wins` posílá `created_at` podle času dokončení výsledku. Zobrazení formátuje čas pro Prahu včetně hodiny a minuty.
